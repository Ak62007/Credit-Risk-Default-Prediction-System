# URL of the real credit_risk /predict API (server-side only — never exposed to the browser).
# Local dev default matches `uv run uvicorn credit_risk.api.main:app --reload` / the Docker image.
PREDICT_API_URL=http://127.0.0.1:8000/predict
